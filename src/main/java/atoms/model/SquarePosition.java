package atoms.model;

/**
 * Position of a square on a chess board.
 * @param row The index of the row.
 * @param column The index of the column.
 */
public record SquarePosition (int row, int column) {}
